[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn api.chat:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/healthz"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
